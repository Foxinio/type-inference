type t ('a, 'b, 'c, 'd) =
	| A : 'a
	| B : 'b
	| C : 'c
	| D : 'd

type t1 = | T1 : unit
type t2 = | T2 : unit
type t3 = | T3 : unit
type t4 = | T4 : unit

let f a =
	match a with
	| A a => ()
	| B b => ()
	| C c => ()
	| D d => ()
	end

let g _ =
	if true
	then if true then A(T1 ()) else B(T2 ())
	else if true then C(T3 ()) else D(T4 ())

in

f (g ())
